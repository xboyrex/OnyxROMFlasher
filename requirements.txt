# ROM Flasher Onyx — Python runtime dependencies
#
# Python standard-library modules used by main.py:
#   json, os, re, shutil, subprocess, tempfile, threading, time, zipfile
#
# Third-party dependency:
#   pywebview provides the desktop WebView bridge used by index.html <-> main.py.
#
# Install:
#   python -m pip install -r requirements.txt

pywebview>=5.0,<7
