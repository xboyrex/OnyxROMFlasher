# Onyx ROM Flasher V1.2 — native Windows GUI
#
# The GUI uses Python's built-in Tkinter/ttk.
# No pywebview, WebView2, or browser runtime is required.
#
# PyInstaller is installed by the GitHub Actions workflow.
